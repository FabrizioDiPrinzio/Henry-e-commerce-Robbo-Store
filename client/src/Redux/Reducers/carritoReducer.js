import * as actionTypes from '../Actions/actionTypes';

const urlBack = process.env.REACT_APP_API_URL;

const initialState = { carritoActual:[]}

export default function carritoReducer(state = initialState, action) {
	switch (action.type) {
        default :
        return state
        }
}
